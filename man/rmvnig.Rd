% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samples_generator.R
\name{rmvnig}
\alias{rmvnig}
\title{Generate censored matrix-variate normal-inverse Gaussian samples}
\usage{
rmvnig(n, cens, Ind, M, U, V, A, gamma_tilde = 2)
}
\arguments{
\item{n}{Positive integer. Number of matrices to generate.}

\item{cens}{Numeric scalar in \verb{[0, 1]}. Proportion used to define the censoring threshold.
If \code{cens = 0}, no censoring is applied.}

\item{Ind}{Integer. Type of incomplete-data mechanism:
\itemize{
\item \code{1}: interval censoring;
\item \code{2}: missing values represented by \verb{(-Inf, Inf)};
\item \code{3}: mixture of interval censoring and missing values.
}}

\item{M}{Numeric matrix of dimension \eqn{p \times q}. Location matrix.}

\item{U}{Square numeric matrix of dimension \eqn{p \times p}. Row covariance matrix.}

\item{V}{Square numeric matrix of dimension \eqn{q \times q}. Column covariance matrix.}

\item{A}{Numeric matrix of dimension \eqn{p \times q}. Skewness matrix.}

\item{gamma_tilde}{Positive numeric scalar. Shape parameter of the inverse Gaussian latent variable.
Default is \code{2}.}
}
\value{
A list with:
\describe{
\item{X.cens}{Array containing censored or observed data.}
\item{cc}{Censoring indicator array. Entries equal to \code{1} indicate censored/missing values.}
\item{LS}{Array containing upper censoring bounds.}
\item{X.or}{Original complete generated data before censoring.}
}
}
\description{
Generates random matrices from a matrix-variate normal-inverse Gaussian-type
model using the stochastic representation
}
\details{
\deqn{
X = M + W A + \sqrt{W} Z,
}

where \eqn{Z \sim MN_{p \times q}(0, U, V)} and
\eqn{W \sim IG(1, \tilde{\gamma})}.

After generating the complete data, the function introduces censoring or
missingness based on the empirical \code{cens} quantile.
}
\keyword{internal}
