% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvst.R
\name{dmvst}
\alias{dmvst}
\title{Density of the multivariate skew-\eqn{t} distribution}
\usage{
dmvst(y, mu, Sigma, lambda, nu, epsilon = 1e-08, log = FALSE)
}
\arguments{
\item{y}{Numeric vector of observed values.}

\item{mu}{Numeric location vector with the same length as \code{y}.}

\item{Sigma}{Positive definite scale matrix with dimensions
\code{length(y)} by \code{length(y)}.}

\item{lambda}{Numeric skewness vector with the same length as \code{y}.}

\item{nu}{Positive scalar. Degrees of freedom.}

\item{epsilon}{Numerical tolerance for positive-definiteness corrections.}

\item{log}{Logical. If \code{TRUE}, returns the log-density.}
}
\value{
A numeric scalar containing the density, or the log-density when
\code{log = TRUE}.
}
\description{
Evaluates the probability density or log-density of the multivariate
skew-\eqn{t} kernel used by the matrix-variate skew-\eqn{t} model. The
distribution combines a skew-normal component with a Gamma scale mixture,
allowing both asymmetry and heavy tails. Matrix-variate skew-\eqn{t}
observations are handled by \code{\link[=loglik_mvst]{loglik_mvst()}}, which vectorizes each \code{p} by
\code{q} slice before calling this density.
}
\examples{
Y <- matrix(seq(-0.2, 0.9, length.out = 12), 3, 4)
A <- matrix(seq(0.1, 1.2, length.out = 12), 3, 4)
dmvst(
  y = as.vector(Y), mu = rep(0, 12), Sigma = diag(12),
  lambda = as.vector(A), nu = 5
)
}
\seealso{
Other MVCens density functions:
\code{\link[=dmvren]{dmvren()}},
\code{\link[=dmvrsn]{dmvrsn()}},
\code{\link[=dmvsn]{dmvsn()}}
}
\concept{MVCens density functions}
