% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvrsn.R
\name{mvrsn_monte_carlo}
\alias{mvrsn_monte_carlo}
\title{Run a reproducible Monte Carlo audit of the MVRSN ECM estimator}
\usage{
mvrsn_monte_carlo(
  sample_sizes = c(50, 100, 200, 400, 800, 1600),
  replications = 200,
  truth = mvrsn_article_parameters(),
  max_iter = 200,
  tol = 1e-06,
  seed = 123,
  verbose = FALSE
)
}
\arguments{
\item{sample_sizes}{Integer vector of sample sizes.}

\item{replications}{Number of Monte Carlo replications per sample size.}

\item{truth}{Optional list with M, A, Sigma, and Psi. Defaults to article
parameters.}

\item{max_iter}{Maximum ECM iterations for each fit.}

\item{tol}{ECM convergence tolerance.}

\item{seed}{Random seed.}

\item{verbose}{Print progress and ECM iterations.}
}
\value{
A list with per-replication results and a sample-size summary.
}
\description{
This function is the practical bridge between theory and evidence. It
repeatedly generates data from the stochastic representation, estimates the
parameters with the ECM algorithm, and measures whether estimates approach
the true values as n grows. For a quick smoke test use small values such as
\code{sample_sizes = c(10, 20)} and \code{replications = 2}; for paper-grade evidence
use the sample sizes and replication count described in the article.
}
