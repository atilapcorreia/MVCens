% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvst.R
\name{mvst_ecm}
\alias{mvst_ecm}
\title{ECM estimation for the matrix-variate skew-t model}
\usage{
mvst_ecm(
  X,
  nu = 4,
  precision = 1e-08,
  max_iter = 50,
  get.nu = TRUE,
  epsilon = 1e-08,
  nu_bounds = c(2.01, 150)
)
}
\arguments{
\item{X}{Numeric array with dimensions \eqn{p \times q \times n}.}

\item{nu}{Degrees-of-freedom value greater than 2. The lower bound keeps the
variance finite for covariance estimation.}

\item{precision}{Positive scalar. Convergence tolerance.}

\item{max_iter}{Positive integer. Maximum number of ECM iterations.}

\item{get.nu}{Logical. If \code{TRUE}, updates \code{nu} by one-dimensional likelihood
maximization at each iteration. If \code{FALSE}, keeps \code{nu} fixed.}

\item{epsilon}{Numerical tolerance for covariance regularization.}

\item{nu_bounds}{Numeric vector of length two. Lower and upper bounds used
when optimizing \code{nu}; the lower bound must be greater than 2.}
}
\value{
An object of class \code{"MVST.ECM"} containing:
\describe{
\item{M, mu}{Estimated location matrix. \code{M} is the canonical name and
\code{mu} is retained as a compatibility alias.}
\item{A}{Estimated skewness matrix.}
\item{Sigma}{Estimated row scale matrix.}
\item{Psi}{Estimated column scale matrix.}
\item{nu}{Estimated or fixed degrees-of-freedom parameter.}
\item{loglik}{Final log-likelihood value.}
\item{loglik_history}{Sequence of log-likelihood values.}
\item{BIC}{Bayesian information criterion.}
\item{iterations, iter}{Number of iterations performed. \code{iter} is a
compatibility alias.}
\item{converged}{Logical value indicating whether convergence was reached.}
\item{criterion}{Final relative log-likelihood change.}
\item{monotone}{Whether the likelihood history avoided material decreases.}
\item{monotone_drops}{Recorded likelihood decreases beyond tolerance.}
\item{normalize_Psi}{Always \code{TRUE}; \code{Psi} uses the normalized representation.}
\item{npar}{Parameter count used in the BIC.}
}
}
\description{
Fits a complete-data matrix-variate skew-t model by an ECM algorithm.
}
\keyword{internal}
