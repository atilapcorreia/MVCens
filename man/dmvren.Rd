% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvren.R
\name{dmvren}
\alias{dmvren}
\title{Density of the matrix-variate row exponential-normal distribution}
\usage{
dmvren(
  X,
  M,
  A,
  Sigma,
  Psi,
  lambda = NULL,
  log = FALSE,
  eig_floor = 1e-10,
  q_policy = c("warn", "strict", "regularize")
)
}
\arguments{
\item{X}{A numeric \code{p} by \code{q} observation matrix.}

\item{M}{A numeric \code{p} by \code{q} location matrix.}

\item{A}{A numeric \code{p} by \code{q} row-specific latent-effect matrix.}

\item{Sigma}{A symmetric positive-definite \code{p} by \code{p} row covariance
matrix.}

\item{Psi}{A symmetric positive-definite \code{q} by \code{q} column covariance
matrix.}

\item{lambda}{An optional numeric vector of length \code{p} containing finite,
strictly positive exponential rate parameters. When \code{NULL}, a vector of
ones is used.}

\item{log}{A logical scalar indicating whether the log-density should be
returned. The default is \code{FALSE}.}

\item{eig_floor}{A positive numeric scalar specifying the minimum numerical
eigenvalue allowed for the matrix \code{Q}. The default is \code{1e-10}.}

\item{q_policy}{Character string indicating how to handle cases in which
the smallest eigenvalue of \code{Q} is less than \code{eig_floor}.
Available options are \code{"warn"}, which regularizes \code{Q} and
issues a warning; \code{"strict"}, which stops with an error; and
\code{"regularize"}, which applies the regularization silently.}
}
\value{
A numeric scalar containing the MVREN density evaluated at \code{X}.
When \code{log = TRUE}, the corresponding log-density is returned.
}
\description{
Computes the density or log-density of the matrix-variate row
exponential-normal (MVREN) distribution at a matrix-valued observation.
The MVREN model extends the matrix-variate normal distribution by
introducing independent, nonnegative row-specific latent effects, allowing
different rows of the observation matrix to exhibit distinct directions
and degrees of asymmetry.
}
