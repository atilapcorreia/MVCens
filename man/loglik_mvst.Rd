% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvst.R
\name{loglik_mvst}
\alias{loglik_mvst}
\title{Matrix-variate skew-t log-likelihood}
\usage{
loglik_mvst(nu, X, muM, AM, SigmaM, PsiM, epsilon = 1e-08)
}
\arguments{
\item{nu}{Positive scalar. Degrees of freedom.}

\item{X}{Numeric array with dimensions \eqn{p \times q \times n}.}

\item{muM}{Location matrix of dimension \eqn{p \times q}.}

\item{AM}{Skewness matrix of dimension \eqn{p \times q}.}

\item{SigmaM}{Row scale matrix of dimension \eqn{p \times p}.}

\item{PsiM}{Column scale matrix of dimension \eqn{q \times q}.}

\item{epsilon}{Numerical tolerance for positive-definiteness corrections.}
}
\value{
A finite numeric scalar containing the total log-likelihood.
}
\description{
Computes the log-likelihood for complete matrix-variate skew-\eqn{t} data.
The MVST construction extends the skew-normal matrix model with a Gamma
scale mixture, so that \code{A} controls asymmetry while \code{nu} controls tail
weight. Each \code{p} by \code{q} observation in \code{X} is vectorized, using the
Kronecker row and column scale structure implied by \code{SigmaM} and \code{PsiM}, and
evaluated by \code{\link[=dmvst]{dmvst()}}.
}
\examples{
x <- array(seq(-0.2, 2.1, length.out = 24), dim = c(3, 4, 2))
A <- matrix(seq(0.1, 1.2, length.out = 12), 3, 4)
loglik_mvst(
  nu = 5, X = x, muM = matrix(0, 3, 4),
  AM = A, SigmaM = diag(3), PsiM = diag(4)
)
}
\seealso{
Other MVCens likelihood functions:
\code{\link[=loglik_mvn]{loglik_mvn()}},
\code{\link[=loglik_mvnc]{loglik_mvnc()}},
\code{\link[=loglik_mvren]{loglik_mvren()}},
\code{\link[=loglik_mvrsn]{loglik_mvrsn()}},
\code{\link[=loglik_mvsn]{loglik_mvsn()}},
\code{\link[=loglik_mvsnc]{loglik_mvsnc()}}
}
\concept{MVCens likelihood functions}
