% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samples_generator.R
\name{rmvvg}
\alias{rmvvg}
\title{Generate random matrices from a matrix-variate variance-gamma distribution}
\usage{
rmvvg(n, M, A, U, V, rate = 1)
}
\arguments{
\item{n}{Positive integer. Number of random matrices to generate.}

\item{M}{Numeric matrix of dimension \eqn{p \times q}. Location matrix.}

\item{A}{Numeric matrix of dimension \eqn{p \times q}. Skewness or drift matrix.}

\item{U}{Square positive definite matrix. Row covariance matrix.}

\item{V}{Square positive definite matrix. Column covariance matrix.}

\item{rate}{Positive finite scalar. Rate parameter of the exponential
distribution used for \eqn{W}. Default is \code{1}.}
}
\value{
A numeric array of dimension
\eqn{p \times q \times number_of_samples}, where each slice
\code{X_array[, , i]} is one generated matrix.
}
\description{
Generates random matrices from the stochastic representation
\deqn{
  X = M + W A + \sqrt{W} V,
}
where \eqn{W \sim \mathrm{Exp}(\mathrm{rate})} and
\eqn{V \sim \mathcal{N}_{p \times q}(0, \Sigma, \Psi)}.
}
\details{
For each sample, the function generates
\eqn{W \sim \mathrm{Exp}(\mathrm{rate})} and a standard normal matrix
\eqn{Z \in \mathbb{R}^{p \times q}}. Then it constructs
\eqn{V = L_\Sigma Z L_\Psi^\top}, where \eqn{L_\Sigma} and
\eqn{L_\Psi} are Cholesky factors of \eqn{\Sigma} and \eqn{\Psi},
respectively.

The generated observation is then computed as
\deqn{
  X_i = M + W_i A + \sqrt{W_i} V_i.
}
}
\keyword{internal}
