% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvren.R
\name{mvren_ecm}
\alias{mvren_ecm}
\title{ECM estimation for the MVREN distribution}
\usage{
mvren_ecm(
  X,
  max_iter = 200,
  precision = 1e-06,
  normalize_Psi = TRUE,
  M_init = NULL,
  A_init = NULL,
  Sigma_init = NULL,
  Psi_init = NULL,
  verbose = TRUE,
  eig_floor = 1e-08,
  q_policy = c("warn", "strict", "regularize"),
  monotone_tol = 1e-07,
  max_backtracks = 12L,
  moment_max_retries = 4L,
  progress_callback = NULL
)
}
\arguments{
\item{X}{A numeric three-dimensional sample array with dimensions \code{p} by
\code{q} by \code{n}.}

\item{max_iter}{A positive integer specifying the maximum number of ECM
iterations. The default is \code{200}.}

\item{precision}{A positive numeric scalar specifying the relative log-likelihood
convergence tolerance. The default is \code{1e-6}.}

\item{normalize_Psi}{A logical scalar indicating whether \code{Psi} should be
rescaled toward a unit-determinant representation. Eigenvalue flooring
applied after rescaling may produce a small numerical deviation from
\code{det(Psi) = 1}. The default is \code{TRUE}.}

\item{M_init}{An optional \code{p} by \code{q} starting value for \code{M}.}

\item{A_init}{An optional \code{p} by \code{q} starting value for \code{A}.}

\item{Sigma_init}{An optional positive-definite \code{p} by \code{p} starting value
for \code{Sigma}.}

\item{Psi_init}{An optional positive-definite \code{q} by \code{q} starting value for
\code{Psi}.}

\item{verbose}{A logical scalar indicating whether iteration information
should be printed. The default is \code{TRUE}.}

\item{eig_floor}{A positive numeric scalar used as an eigenvalue floor in
covariance and precision-matrix regularization. The default is \code{1e-8}.}

\item{q_policy}{A character string specifying how to handle a matrix \code{Q}
whose smallest eigenvalue is below \code{eig_floor}. Available options are
\code{"warn"}, \code{"strict"}, and \code{"regularize"}.}

\item{monotone_tol}{A nonnegative numeric scalar specifying the largest
numerical decrease in log-likelihood treated as acceptable. The default
is \code{1e-7}.}

\item{max_backtracks}{A nonnegative integer specifying the maximum number of
step halvings attempted when a full update decreases the log-likelihood.
The default is \code{12L}.}

\item{moment_max_retries}{A positive integer specifying the maximum number
of repeated attempts used to obtain valid truncated-normal moments. The
default is \code{4L}.}

\item{progress_callback}{Optional function called after initialization and
after every completed ECM iteration with arguments \code{iteration},
\code{max_iter}, and \code{criterion}. Callback failures are ignored so monitoring
cannot alter the scientific fit.}
}
\value{
An object of class \code{"mvren_fit"}, represented by a list containing:
\describe{
\item{M, A, Sigma, Psi, lambda}{The fitted MVREN parameters.}
\item{loglik}{The final observed-data log-likelihood.}
\item{loglik_history}{The sequence of accepted log-likelihood values.}
\item{iterations}{The number of ECM iterations performed.}
\item{BIC}{The Bayesian information criterion computed by the function.}
\item{converged}{A logical indicator of convergence under \code{precision}.}
\item{criterion}{The final relative log-likelihood change.}
\item{monotone}{A logical indicator of whether decreases larger than
\code{monotone_tol} were avoided.}
\item{monotone_drops}{Any recorded decreases exceeding
\code{monotone_tol}.}
\item{step_sizes}{The accepted step size at each iteration.}
\item{rejected_updates}{The number of full update sequences rejected
after unsuccessful backtracking.}
\item{normalize_Psi}{The requested covariance-identification setting.}
\item{lambda_mode}{Always \code{"fixed"}, retained as an explicit diagnostic.}
\item{npar}{The parameter count used in the BIC calculation.}
}
}
\description{
Fits a matrix-variate row exponential-normal model by an expectation-
conditional-maximization algorithm with optional numerical regularization
and monotonicity-preserving step halving.
}
\details{
In the E-step, each latent vector is represented by a multivariate normal
distribution with covariance \code{Q^{-1}} and mean
\code{Q^{-1}(b_i - lambda)}, truncated to the positive orthant. The subsequent
CM-steps update \code{M}, \code{A}, \code{Psi}, and \code{Sigma}; the rate vector is fixed at
one for every row throughout the fit.
Candidate updates that reduce the observed-data log-likelihood beyond
\code{monotone_tol} are subjected to step halving. Numerical eigenvalue flooring
can make the implemented procedure a stabilized ECM-type algorithm rather
than an exact unregularized ECM iteration.
}
\keyword{internal}
