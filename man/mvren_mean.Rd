% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvren.R
\name{mvren_mean}
\alias{mvren_mean}
\title{Compute the mean matrix of an MVREN distribution}
\usage{
mvren_mean(M, A, lambda = NULL)
}
\arguments{
\item{M}{A numeric \code{p} by \code{q} location matrix.}

\item{A}{A numeric \code{p} by \code{q} latent-effect matrix.}

\item{lambda}{An optional numeric vector of length \code{p} containing finite,
strictly positive exponential rate parameters. When \code{NULL}, a vector of
ones is used. The high-level MVREN API (\code{mv_random()} and \code{mv_fit()})
fixes this vector to ones for identifiability; non-unit values here are
supported only to evaluate a supplied general parameterization.}
}
\value{
A numeric \code{p} by \code{q} matrix containing the mean of the MVREN
distribution.
}
\description{
Computes the mean matrix of a matrix-variate row exponential-normal
distribution from its location matrix, latent-effect matrix, and
row-specific exponential rate parameters. In this model, independent
exponential latent variables produce row-specific mean shifts through
\verb{X = M + W A + V}, where \code{W} is diagonal and \code{V} is matrix-normal noise.
}
\details{
For the identified model used by \code{mv_random()} and \code{mv_fit()}, use
the default unit-rate vector. The optional argument is retained here to
evaluate moments for an explicitly supplied general parameterization.
}
\examples{
M <- matrix(0, 3, 4)
A <- matrix(seq(0.1, 1.2, length.out = 12), 3, 4)
mvren_mean(M, A)
}
\seealso{
Other MVCens MVREN functions:
\code{\link[=dmvren]{dmvren()}},
\code{\link[=loglik_mvren]{loglik_mvren()}},
\code{\link[=mvren_article_parameters]{mvren_article_parameters()}},
\code{\link[=mvren_covariances]{mvren_covariances()}}
}
\concept{MVCens MVREN functions}
