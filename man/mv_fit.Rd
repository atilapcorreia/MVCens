% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_fit.R
\name{mv_fit}
\alias{mv_fit}
\title{Fit a registered matrix-variate model by ECM}
\usage{
mv_fit(
  model,
  X = NULL,
  cc = NULL,
  LS = NULL,
  precision = 1e-06,
  max_iter = 500L,
  ...
)
}
\arguments{
\item{model}{One estimable model name. Matching is case-insensitive.}

\item{X}{Numeric array of dimensions \code{p} by \code{q} by \code{n}, containing the
matrix-valued observations. The deprecated \code{samples} name may be supplied
through \code{...} for backward compatibility.}

\item{cc}{Optional censoring indicator array with the same dimensions as
\code{X}; \code{1} marks censored/missing entries and \code{0} marks observed entries.}

\item{LS}{Optional array of upper censoring limits, with the same dimensions
as \code{X}. Required by censored models together with \code{cc}.}

\item{precision}{Positive finite ECM convergence tolerance.}

\item{max_iter}{Positive integer maximum number of ECM iterations.}

\item{...}{Model-specific controls and initial values, for example \code{nu},
\code{M_init}, \code{A_init}, \code{Sigma_init}, \code{Psi_init}, \code{normalize_Psi}, or
\code{verbose}.}
}
\value{
A model-specific fit object containing estimated parameters and,
where implemented, the likelihood path, iteration count, convergence
flag, BIC, and numerical diagnostics.
}
\description{
Validates the common fit contract and dispatches to the model-specific
EM-type implementation described by the corresponding model article. \code{X}
must contain \code{n} matrix observations stored as a \code{p} by \code{q} by \code{n} array.
Matrix-normal and skew-normal censored fits use ECM updates built around
truncated moments, \code{MVST} uses an ECME-style heavy-tailed skew model, and
\code{MVRSN}/\code{MVREN} use row-specific latent-variable ECM kernels. The model name
is case-insensitive and the scientific update equations remain in the model
kernel.
}
\details{
Models currently fitted by this interface are \code{MVN}, \code{MVNC}, \code{MVSN},
\code{MVSNC}, \code{MVST}, \code{MVRSN}, and \code{MVREN}. \code{MVNIG} and \code{MVVG} are generation-only
models in this package and fail closed with an informative error when passed
to \code{mv_fit()}.

ECM iterations are kept sequential because their update order is
part of each model's scientific specification. The function is
observational with respect to the input data: it returns a fit object and
does not modify \code{X}, \code{cc}, or \code{LS} in place. Generation-only models fail
closed instead of silently switching to another estimator.
}
\examples{
set.seed(123)
M <- matrix(0, 3, 4)
x <- mv_random(
  "MVN", n = 8, M = M,
  Sigma = diag(3), Psi = diag(4)
)
fit <- mv_fit("MVN", X = x, max_iter = 2)
names(fit)
}
\seealso{
\code{\link[=mv_random]{mv_random()}}, \code{\link[=mv_monte_carlo]{mv_monte_carlo()}}

Other MVCens main interface:
\code{\link[=mv_monte_carlo]{mv_monte_carlo()}},
\code{\link[=mv_random]{mv_random()}}
}
\concept{MVCens main interface}
