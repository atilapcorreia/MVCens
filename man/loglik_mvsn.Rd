% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels_mvsn_mvsnc.R
\name{loglik_mvsn}
\alias{loglik_mvsn}
\title{Matrix-variate skew-normal log-likelihood}
\usage{
loglik_mvsn(X, muM, AM, SigmaM, PsiM, epsilon = 1e-08)
}
\arguments{
\item{X}{Numeric array with dimensions \eqn{p \times q \times n}.}

\item{muM}{Location matrix of dimension \eqn{p \times q}.}

\item{AM}{Skewness matrix of dimension \eqn{p \times q}.}

\item{SigmaM}{Row covariance matrix of dimension \eqn{p \times p}.}

\item{PsiM}{Column covariance matrix of dimension \eqn{q \times q}.}

\item{epsilon}{Numerical tolerance for covariance regularization.}
}
\value{
A finite numeric scalar containing the total log-likelihood.
}
\description{
Computes the total log-likelihood for complete matrix-variate skew-normal
data. The model uses the stochastic representation \verb{X = M + W A + V}, where
\code{W} is a positive half-normal latent variable and \code{V} is matrix-normal noise
with separable row and column covariance. Each \code{p} by \code{q} observation is
vectorized and evaluated through the corresponding multivariate
skew-normal density with Kronecker covariance structure.
}
\examples{
x <- array(seq(-0.2, 2.1, length.out = 24), dim = c(3, 4, 2))
A <- matrix(seq(0.1, 1.2, length.out = 12), 3, 4)
loglik_mvsn(
  x, muM = matrix(0, 3, 4), AM = A,
  SigmaM = diag(3), PsiM = diag(4)
)
}
\seealso{
Other MVCens likelihood functions:
\code{\link[=loglik_mvn]{loglik_mvn()}},
\code{\link[=loglik_mvnc]{loglik_mvnc()}},
\code{\link[=loglik_mvren]{loglik_mvren()}},
\code{\link[=loglik_mvrsn]{loglik_mvrsn()}},
\code{\link[=loglik_mvsnc]{loglik_mvsnc()}},
\code{\link[=loglik_mvst]{loglik_mvst()}}
}
\concept{MVCens likelihood functions}
