% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvren.R
\name{mvren_covariances}
\alias{mvren_covariances}
\title{Compute row and column covariance summaries of an MVREN distribution}
\usage{
mvren_covariances(M, A, Sigma, Psi, lambda = NULL)
}
\arguments{
\item{M}{A numeric \code{p} by \code{q} location matrix. The covariance summaries do
not depend on \code{M}; it is used to establish and validate the model
dimensions.}

\item{A}{A numeric \code{p} by \code{q} latent-effect matrix.}

\item{Sigma}{A symmetric positive-definite \code{p} by \code{p} row covariance
matrix.}

\item{Psi}{A symmetric positive-definite \code{q} by \code{q} column covariance
matrix.}

\item{lambda}{An optional numeric vector of length \code{p} containing finite,
strictly positive exponential rate parameters. When \code{NULL}, a vector of
ones is used. The high-level MVREN API fixes this vector to ones; non-unit
values here are for evaluating a supplied general parameterization.}
}
\value{
A list containing:
\describe{
\item{row}{A \eqn{p \times p} matrix equal to
\eqn{\mathbb{E}\left[
  (\mathbf{X}-\mathbb{E}(\mathbf{X}))
  (\mathbf{X}-\mathbb{E}(\mathbf{X}))^{\top}
  \right]}.}
\item{column}{A \eqn{q \times q} matrix equal to
\eqn{\mathbb{E}\left[
  (\mathbf{X}-\mathbb{E}(\mathbf{X}))^{\top}
  (\mathbf{X}-\mathbb{E}(\mathbf{X}))
  \right]}.}
}
}
\description{
Computes the row and column covariance summaries associated with a
matrix-variate row exponential-normal distribution from its latent-effect
matrix, row and column covariance matrices, and row-specific exponential
rate parameters. These summaries combine the separable matrix-normal
covariance contribution with the variability induced by independent
exponential row effects. They describe row-wise and column-wise second
moments of the matrix-valued observation, not a fitted model.
}
\details{
For the identified model used by \code{mv_random()} and \code{mv_fit()}, use
the default unit-rate vector. The optional argument is retained here to
evaluate covariance summaries for an explicitly supplied general
parameterization.
}
\examples{
M <- matrix(0, 3, 4)
A <- matrix(seq(0.1, 1.2, length.out = 12), 3, 4)
mvren_covariances(
  M = M, A = A,
  Sigma = diag(3), Psi = diag(4)
)
}
\seealso{
Other MVCens MVREN functions:
\code{\link[=dmvren]{dmvren()}},
\code{\link[=loglik_mvren]{loglik_mvren()}},
\code{\link[=mvren_article_parameters]{mvren_article_parameters()}},
\code{\link[=mvren_mean]{mvren_mean()}}
}
\concept{MVCens MVREN functions}
