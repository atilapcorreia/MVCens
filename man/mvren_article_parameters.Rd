% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mvren.R
\name{mvren_article_parameters}
\alias{mvren_article_parameters}
\title{Predefined parameter set for the MVREN distribution}
\usage{
mvren_article_parameters(normalize_Psi = TRUE)
}
\arguments{
\item{normalize_Psi}{Logical scalar indicating whether \code{Psi} should be
normalized to have determinant one. The corresponding reciprocal scaling
is applied to \code{Sigma} so that the Kronecker-product covariance
structure remains unchanged. The default is \code{TRUE}.}
}
\value{
A list containing the matrices \code{M}, \code{A}, \code{Sigma},
and \code{Psi}, together with the exponential-rate vector \code{lambda}.
}
\description{
Constructs a predefined collection of \code{3} by \code{4} parameters for the MVREN
Monte Carlo setting described in the row exponential-normal article. The
setting is intended for simulation studies, numerical experiments, and
reproducible examples involving row-specific exponential latent effects.
}
\details{
The returned parameter set uses the identified MVREN convention
\code{lambda_i = 1} for every row. The \code{normalize_Psi} option concerns only the
separate row/column covariance scale constraint; rows of \code{A} are not
normalized. The parameter values are provided as a reproducible reference
set; they are not fitted or estimated by this function.
}
\examples{
pars <- mvren_article_parameters()
names(pars)
det(pars$Psi)
}
\seealso{
Other MVCens MVREN functions:
\code{\link[=dmvren]{dmvren()}},
\code{\link[=loglik_mvren]{loglik_mvren()}},
\code{\link[=mvren_covariances]{mvren_covariances()}},
\code{\link[=mvren_mean]{mvren_mean()}}
}
\concept{MVCens MVREN functions}
