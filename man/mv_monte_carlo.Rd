% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monte_carlo_engine.R
\name{mv_monte_carlo}
\alias{mv_monte_carlo}
\title{Run a Monte Carlo campaign for a registered MVCens model}
\usage{
mv_monte_carlo(
  model,
  sample_sizes = c(50, 100, 200, 400),
  replications = 200,
  truth,
  workers = NULL,
  precision = 1e-06,
  max_iter = 500L,
  seed = 123L,
  progress_dir = NULL,
  ...
)
}
\arguments{
\item{model}{One of \code{MVN}, \code{MVNC}, \code{MVSN}, \code{MVSNC}, \code{MVST}, \code{MVRSN}, or
\code{MVREN}.}

\item{sample_sizes}{Positive integer vector of sample sizes.}

\item{replications}{Positive number of replications per sample size.}

\item{truth}{Named list containing the data-generating parameters, typically
\code{M}, \code{Sigma}, \code{Psi}, and, for skewed or latent-effect models, \code{A}.}

\item{workers}{Number of parallel workers; \code{NULL} uses all logical CPUs.}

\item{precision}{ECM convergence tolerance.}

\item{max_iter}{Maximum ECM iterations.}

\item{seed}{Integer random seed.}

\item{progress_dir}{Optional directory used to publish transient per-worker
MVREN ECM iteration progress. Files are removed when each replication
finishes. \code{NULL} disables progress reporting.}

\item{...}{Model-specific options such as \code{cens} or \code{Ind}; censored
campaigns use \code{cc = 1} for censored entries in generated samples.}
}
\value{
A list with per-replication \code{results}, grouped \code{summary}, and the
supplied \code{truth}. The summary is descriptive and does not change model
defaults or fitted estimates.
}
\description{
Runs repeated generate-and-fit experiments for a registered estimable model.
This is intended to reproduce the Monte Carlo style used in the accompanying
matrix-variate articles: data are generated from a known matrix-valued truth,
the corresponding EM-type estimator is fitted, and estimation error is
summarized across replications. Independent replications are distributed
across all available logical CPU threads by default. ECM iterations remain
sequential, preserving their mathematical update order. Random streams are
reproducible across worker counts for a fixed seed.
}
\examples{
truth <- list(
  M = matrix(0, 3, 4),
  Sigma = diag(3),
  Psi = diag(4)
)
study <- mv_monte_carlo(
  "MVN", sample_sizes = 5, replications = 1, truth = truth,
  workers = 1, max_iter = 1, seed = 123
)
study$summary
}
\seealso{
Other MVCens main interface:
\code{\link[=mv_fit]{mv_fit()}},
\code{\link[=mv_random]{mv_random()}}
}
\concept{MVCens main interface}
