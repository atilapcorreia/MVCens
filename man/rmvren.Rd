% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/samples_generator.R
\name{rmvren}
\alias{rmvren}
\title{Generate random matrices from the MVREN distribution}
\usage{
rmvren(n, M, A, Sigma, Psi, lambda = NULL, return_latent = FALSE)
}
\arguments{
\item{n}{Number of matrices to generate.}

\item{M}{Location matrix of dimension \code{p} by \code{q}.}

\item{A}{Row-specific latent-effect matrix of dimension \code{p} by \code{q}.}

\item{Sigma}{Positive-definite row covariance matrix of dimension \code{p} by
\code{p}.}

\item{Psi}{Positive-definite column covariance matrix of dimension \code{q} by
\code{q}.}

\item{lambda}{Optional positive rate vector of length \code{p}. When \code{NULL}, a
vector of ones is used.}

\item{return_latent}{If \code{TRUE}, return the generated latent exponential
vectors together with the observations.}
}
\value{
If \code{return_latent = FALSE}, an array of dimension \code{p} by \code{q} by \code{n}.
Otherwise, a list containing \code{X} and \code{W}, where \code{W} is an \code{n} by \code{p}
matrix of latent exponential variables.
}
\description{
This function implements the stochastic representation directly:
\deqn{
X_i = M + \operatorname{diag}(w_i)A + V_i,
}
where the components of \eqn{w_i} are mutually independent exponential
random variables with rates given by \code{lambda}, and \eqn{V_i} follows a
matrix-normal distribution with covariance \verb{Psi \\\%x\\\% Sigma} after
vectorization. It is the reference generator used by the Monte Carlo audit.
}
\keyword{internal}
