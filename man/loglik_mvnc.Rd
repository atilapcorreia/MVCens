% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels_mvn_mvnc.R
\name{loglik_mvnc}
\alias{loglik_mvnc}
\title{Censored matrix-variate normal log-likelihood}
\usage{
loglik_mvnc(cc, LS, samples, M, Sigma, Psi, epsilon = 1e-08)
}
\arguments{
\item{cc}{Censoring indicator array with the same dimensions as \code{samples}.
Entries equal to \code{1} indicate censored or missing values; entries equal to
\code{0} indicate observed values.}

\item{LS}{Array of upper censoring limits with the same dimensions as
\code{samples}. Censored entries must satisfy \code{samples <= LS}.}

\item{samples}{Numeric array with dimensions \eqn{p \times q \times n}.}

\item{M}{Location matrix of dimension \eqn{p \times q}.}

\item{Sigma}{Row covariance matrix of dimension \eqn{p \times p}.}

\item{Psi}{Column covariance matrix of dimension \eqn{q \times q}.}

\item{epsilon}{Positive tolerance used for numerical stabilization.}
}
\value{
A finite numeric scalar containing the censored-data log-likelihood.
}
\description{
Computes the observed-data log-likelihood for censored and/or missing
matrix-variate normal data, following the matrix-normal censored-data
formulation based on truncated moments. Fully observed matrices are evaluated
through the matrix-normal density. Fully censored matrices are evaluated
through the matrix-normal probability, and partially censored matrices are
handled by conditioning on the observed entries.
}
\examples{
M <- matrix(0, 3, 4)
x <- array(seq(-0.2, 2.1, length.out = 24), dim = c(3, 4, 2))
cc <- array(0, dim = dim(x))
cc[1, 1, 2] <- 1
limits <- array(Inf, dim = dim(x))
limits[1, 1, 2] <- 1.2
loglik_mvnc(cc, limits, x, M, diag(3), diag(4))
}
\seealso{
Other MVCens likelihood functions:
\code{\link[=loglik_mvn]{loglik_mvn()}},
\code{\link[=loglik_mvren]{loglik_mvren()}},
\code{\link[=loglik_mvrsn]{loglik_mvrsn()}},
\code{\link[=loglik_mvsn]{loglik_mvsn()}},
\code{\link[=loglik_mvsnc]{loglik_mvsnc()}},
\code{\link[=loglik_mvst]{loglik_mvst()}}
}
\concept{MVCens likelihood functions}
